version: 3
automerge: true
projects:
- name: clearbit
  dir: s3
  workspace: dev
  terraform_version: v0.13.5
  workflow: myworkflow
workflows:
  myworkflow:
    plan:
      steps:
      #- run: terraform init -input=false -no-color
      - run: terraform init
      - run: terraform workspace new dev
      - run: terraform workspace select -no-color dev 
        #make bootstrap
      #- run: terraform workspace select -no-color $WORKSPACE
      - run: terraform plan -no-color dev 
        #make plan
    apply:
      steps:
      #- run: terraform apply -no-color $PLANFILE
      - run: terraform apply -no-color dev
        #make apply
